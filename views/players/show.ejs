<%- include("../partials/header") %>

<h1><%= player.fullname %></h1>

<div>
    <img src="<%= player.img %>" alt="">
</div>

<% player.matches.forEach(function(match) { %>
    <li>
        <a href="/matches/<%= match._id %>"><%= match.title %></a>
    </li>
 <% }) %>

<%- include("../partials/footer") %>